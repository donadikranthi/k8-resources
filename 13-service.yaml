apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  selector:
    project: nginx
    component: frontend
    environment: dev
  ports:
    - protocol: TCP
      port: 80 # Service Port
      targetPort: 80 # Container Port